# ALMOND 최종 데모 개발 계획

> 9주차/11주차 사용자 피드백을 반영해 도출된 ALMOND 프로젝트의 최종 데모 개발 계획입니다.
> Claude Code 핸드오프용 컨텍스트 문서입니다.

---

## 1. 제품 방향

- **컨셉**: 홈트 입문자용 자세 코치 — "다중 오류 동시 감지 + 좌우 비대칭 정량화"
- **타겟**: 집에서 맨몸 운동하는 입문자
- **평가 포인트**: 인식 정확도 (기술 완성도)
- **일정**: 3~4주
- **저장 기능**: 없음 — 단일 세션만 (스토리지·DB 도입 금지)

## 2. 동작 구성

데모에는 **스쿼트 + 푸쉬업** 두 가지만 포함.

- 기존 레터럴 레이즈는 **제거** (홈트 컨텍스트 부적합)
- 동작당 4개 신호 감지: 깊이 / 자세 / 정렬 / 좌우 비대칭

## 3. 차별화 포인트

1. **다중 오류 동시 감지** — 어디가 왜 틀렸는지 정확히 표시
2. **좌우 비대칭 정량화** — "왼쪽 무릎 85°, 오른쪽 92°" 같은 측정 지표
3. **단일 인물 락온** — 여러 명이 프레임에 들어와도 본인만 추적

## 4. 목표 리스트 (9개, 순서대로)

### 토대

**G1. 멀티 신호 피드백 구조 리팩토링**
- exercise 인터페이스를 "신호 배열 반환"으로 변경, 레터럴 레이즈 제거
- 검증: 기존 스쿼트가 새 구조에서 동일 작동 (UX 변화 0)

**G2. 오류 위치 시각화 레이어**
- 신호의 `affectedKeypoints`를 받아 해당 관절·뼈를 빨강/노랑 강조
- 검증: 자세 흐트러뜨릴 때 잘못된 부위만 색 변화

### 핵심 감지

**G3. 스쿼트 4신호 감지**
- 깊이 / 무릎 안쪽 모임 / 등 굽음 / 좌우 비대칭
- 검증: 4가지 오류 케이스 각각에 대해 해당 신호만 발화

**G4. 푸쉬업 기본 추가**
- 푸쉬업 exercise 정의, HomeScreen 카드 추가
- 검증: 카운팅 정확, 흐름이 스쿼트와 동일

**G5. 푸쉬업 4신호 감지**
- 깊이 / 엉덩이 처짐 또는 들림 / 팔꿈치 벌어짐 / 좌우 비대칭
- 검증: G3와 동일 방식

### 견고함

**G6. 단일 인물 락온**
- MultiPose로 전환, 면적 기준 가장 큰 인물 1명만 추적, "LOCKED" 배지 표시
- 검증: 두 명 들어와도 한 명만 골격 그려짐
- 출처: 11주차 피드백 "본인만 인식돼야 안심"

**G7. 카메라 위치 자동 판별**
- 어깨 좌표로 정면/측면 감지, 측면 시 좌우 비대칭 비활성화
- 검증: 카메라 위치 바꿔도 부적절한 신호 안 뜸
- 출처: 9주차 피드백 "카메라 위치 정면/측면"

### 마감

**G8. 결과 화면 개선**
- 동작별 통계, 좌우 비대칭 평균, 오류 빈도 TOP 3
- 의존성: G3, G5
- 검증: 실제 세션 끝나면 의미 있는 수치 표시

**G9. 데모 시나리오 폴리싱**
- 로딩, 권한 거부, 감지 실패 엣지 케이스 + 3분 시연 리허설
- 검증: 무결점 시연

> 0~100 정확도 점수 시스템은 의도적으로 제외됨.

---

## 5. G1 상세 설계 (즉시 착수 가능)

### 5.1 Signal 타입

```javascript
{
  id: 'depth_insufficient',          // 식별자 (디버깅/로깅)
  severity: 'info' | 'warning' | 'error',
  affectedKeypoints: [11, 13, 15],   // G2에서 시각화
  message: '더 깊이 내려가세요'      // 사용자 표시 텍스트
}
```

심각도:
- `info`: 동작 큐 ("내려가세요", "올라가세요") — 오류 아님
- `warning`: 경미한 문제 (예: 좌우 차 5~10°)
- `error`: 명확한 오류

### 5.2 Exercise 인터페이스 변경

| 메서드 | 변경 |
|---|---|
| `evaluate(kp, phase)` | **신규** — `{ signals: Signal[] }` 반환 |
| `getDetailedFeedback` | **제거** (evaluate로 흡수) |
| `isGoodPose` | **제거** (signals에 error 없으면 좋은 자세) |
| `detect`, `getDisplayAngle`, `getPhaseLabel`, 메타데이터, `guidePoints` | **유지** |

### 5.3 ExerciseScreen 변경

- `feedbackText` 상태 → `signals` 상태로 교체
- 표시 메시지: signals 중 가장 심각한 것 하나 (error > warning > info)
- `goodFramesRef` 카운트 판정: `signals.some(s => s.severity === 'error')`가 false면 좋은 프레임

### 5.4 스쿼트 마이그레이션 매핑 (기존 동작 1:1 보존)

| 현재 분기 | 새 신호 |
|---|---|
| phase=up, angle<160 → "무릎을 완전히 펴세요" | error, `depth_incomplete_top` |
| phase=up, angle≥160 → "내려가세요!" | info, `cue_descend` |
| phase=down, angle>130 → "더 깊이 내려가세요" | error, `depth_insufficient` |
| phase=down, angle≤130 → "올라가세요!" | info, `cue_ascend` |

### 5.5 손대는 파일

- `src/exercises/index.js`
- `src/screens/ExerciseScreen.js`
- `src/screens/HomeScreen.js`는 EXERCISES 변경으로 자동 반영

### 5.6 검증 기준 (G1 성공 조건)

1. 앱 실행 → 스쿼트만 보임 (레터럴 레이즈 카드 없음)
2. 스쿼트 진행 시 화면 메시지가 기존과 동일하게 표시
3. 결과 화면 자세 정확도(%) 기존과 동일하게 동작
4. 콘솔에서 매 프레임 signals 배열 정상 출력

**G1 핵심**: 내부 구조만 바꾸고 사용자 입장에서 동일 동작. G2~G7이 이 토대 위에 새 기능을 얹는다.

---

## 6. Claude Code 사용 권장 방식

1. 이 파일을 프로젝트 루트에 `PLAN.md`로 저장
2. Claude Code 첫 프롬프트:
   ```
   PLAN.md를 먼저 읽고 G1만 구현해줘. 5.6의 검증 기준 4개 모두 통과해야 함.
   ```
3. G1 완료·검증 후 G2, G3 순으로 **한 번에 하나씩**
4. **G를 묶지 말 것** — 한 G가 끝나야 다음 G의 가정이 명확해짐

## 7. 잊지 말 것 (Claude Code 가드레일)

- 점수 시스템(0~100) 추가하지 말 것 — 의도적 제외
- 데이터 저장(localStorage, DB) 추가하지 말 것
- 동작을 3개 이상 추가하지 말 것
- "더 좋아 보이게" 인접 코드 리팩토링하지 말 것
- 요청되지 않은 추상화·설정 옵션 추가하지 말 것
